import SwiftUI

extension Color {
    static let blackboardBackground = Color(red: 0, green: 0.3, blue: 0)
    static let graphParabola = Color.blue
    static let graphLine = Color.red
    static let graphIntersection = Color.green
}
